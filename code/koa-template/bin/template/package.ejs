
{
  "name": "<%= packageName %>",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "rm -rf test-server-setup && node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.13.1",

    <% if (middleware.body) { %>
      "koa-body": "^4.2.0",
      <% } %>

    <% if (middleware.router) { %>
      "koa-router": "^10.0.0",
      <% } %>

    <% if (middleware.static) { %>
      "koa-static": "^5.0.0",
      <% } %>

    <% if (middleware.views) { %>
      "koa-views": "^7.0.1",
      "pug": "^3.0.2"
      <% } %>
  }
}
